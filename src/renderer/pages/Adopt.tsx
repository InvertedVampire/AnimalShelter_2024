// Adoption page handles the requirements for an adoption
// also serves as location of front end code.
import React from 'react';

export const Adopt:React.FC = () => {
    return <div>This is the adopt page</div>
}