// Reclaim page handles the requirements for animal reclamation
// also serves as location of front end code.
import React from 'react';

export const Reclaim:React.FC = () => {
    return <div>This is the Reclaim page</div>
}